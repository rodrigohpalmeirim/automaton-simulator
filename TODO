fix connections replaced when new one created with same value
fix pressing J toggles pane when writing text
fix short arrows when nodes are too close
fix editing while running
fix tape reset when invalid json string
fix 1 character node names
add speed controls
add initial node control and display
add import and export
add localStorage saving
add json pane toggle button
add JSON free editing mode
add instructions
add undo/redo
add panning
add node renaming
add group selection
add multiple connections in a single arrow
add shadows