fix arrows rendering on top of each other
fix short arrows when nodes are too close
fix connections replaced when new one created with same value
add node renaming
add label moving
add temp node auto-removing when connection canceled